# Git extension aliases
alias adog="glgg"
alias glff="gl --ff-only"

# QOL aliases
alias cl="clear"
alias py="python"

# Docker aliases
alias dcup="docker-compose up"
alias dcupd="dcup -d"
alias dcexec="docker-compose exec"

